import React    from "react";
import template from "./PremiumSection.jsx";

class PremiumSection extends React.Component {
  render() {
    return template.call(this);
  }
}

export default PremiumSection;
